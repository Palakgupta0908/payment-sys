spring:
  application:
        name : user-service
  datasource:
    url: jdbc:mysql://localhost:3306/payment-user
    username: {user}
    password: {pass}

  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQL8Dialect


