spring:
  application:
        name : user-service
  datasource:
    url: jdbc:mysql://localhost:3306/payment-user
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQL8Dialect


